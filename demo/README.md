# Demo workspace

External demo repositories are not vendored in this branch.

The previous `demo/jie_3d_nav` and `demo/lucid-atlas` entries were gitlink
pointers, so they did not contain cloneable source code in this repository.
Keep demo repositories outside this project or add them locally when needed.
